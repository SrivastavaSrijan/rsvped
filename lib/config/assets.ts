export const AssetMap = {
  root: '/assets',
  get Background() {
    return `${this.root}/background.svg`
  },
  get HeroVideo() {
    return `${this.root}/phone-dark.webm`
  },
  get NoEvents() {
    return `${this.root}/no-events.svg`
  },
}
